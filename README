Exemplo do uso do MultiStage do Docker 


1- build da imagem
  docker build -t go-hello:latest .

2- executar a imagem
  docker run --rm go-hello:latest

ou rode baixando a imagem do docker hub
  docker run --rm fabioaspassos/go-hello:latest

Endereco do Docker hub
https://hub.docker.com/r/fabioaspassos/go-hello